# Jarchi 📣 — Changelog / تاریخچه نسخه‌ها

## v4.3
**رفع قطع‌ووصل ابتدای پیج + داکینگ واقعی:**
- معماری پیج از «سوییچ منبع» به «میکس دائمی» تغییر کرد — دیگر هیچ پرشی در شروع تماس نیست
- داکینگ بدون وابستگی به smooth_add (که در Liquidsoap 2.0 موجود نیست): پنل با شروع/پایان هر پیج، ضریب صدای موزیک را تنظیم می‌کند
- میزان کم شدن موزیک هنگام پیج از تنظیمات قابل تغییر شد (0 تا 90 درصد)
**دیگر:** پوشه نصب به /opt/jarchi تغییر کرد (مهاجرت خودکار از مسیر قدیمی) · README به‌روزرسانی شد


## v4.2.1
**رفع باگ SIP (خطای 503):**
- افزودن line=true به رجیستریشن: تماس ورودی از سانترال حالا مطمئناً به endpoint نسبت داده می‌شود (قبلاً فقط تطبیق IP بود و 401→503 می‌داد)
- گاردهای NAT (rtp_symmetric, force_rport, rewrite_contact) روی همه endpoint ها
- دایال‌پلن catch-all: هر شماره‌ای که سانترال بفرستد به پیج می‌رسد
- بازطراحی حالت B: شش اکانت گوشی (101-106) با پسورد مشترک پنل + کدک gsm
**دیگر:** README دوزبانه گیت‌هاب


## v4.0
**قابلیت‌ها:**
- کراس‌فید بین آهنگ‌ها (میکس نرم انتهای آهنگ با ابتدای بعدی، سبک Spotify)
- داکینگ: هنگام پیج/زنگ، موزیک به ۱۵٪ fade می‌شود به‌جای قطع کامل
- 🕌 اذان خودکار: محاسبه روزانه اوقات شرعی (کتابخانه adhan، مختصات قابل تنظیم)
  و پخش خودکار فایل اذان برای صبح/ظهر/مغرب
- برنامه خودکار منبع: تغییر خودکار رادیو/پلی‌لیست/خاموش در ساعت و روزهای مشخص
- جستجوی ایستگاه رادیو از دیتابیس جهانی radio-browser با افزودن یک‌کلیکی
- تاریخچه پیج‌ها در داشبورد (داخلی، زمان، مدت)
- 🎧 Spotify Connect (اختیاری، librespot): سرور به‌عنوان دستگاه پخش اسپاتیفای

## v3.4
**قابلیت‌ها:** حالت پخش درهم (شافل) · seek با کلیک روی نوار پخش ·
۶ ایستگاه پیش‌فرض جهانی جدید · برند Jarchi · کردیت سازنده (Ehsan Abdoli) ·
بخش دونیت با آدرس کیف پول
**رفع باگ:**
- تکرار بی‌پایان یک آهنگ (پارامتر reload=30 هر ۳۰ ثانیه پلی‌لیست را ریست می‌کرد)
- جابه‌جایی دکمه‌های قبلی/بعدی در حالت RTL
- پرش انیمیشن چرخش کاور (بازسازی DOM در هر بروزرسانی وضعیت)

## v3.3
**تغییر معماری (رفع قطعی «هیچی پخش نمی‌شود»):**
- وضعیت (منبع فعال / صدا / آدرس رادیو) از حافظه موتور به فایل‌های state منتقل شد؛
  موتور هر ۲ ثانیه خودش می‌خواند → ری‌استارت موتور دیگر وضعیت را از بین نمی‌برد
  و وابستگی به دستورات شکننده telnet حذف شد

## v3.2
**رفع باگ:**
- تغییر ایستگاه رادیو عملاً اثری نداشت (ورودی http آدرس را فقط موقع استارت
  می‌خواند؛ حالا دستور url/stop/start به خود ورودی داده می‌شود)
- جلوگیری از قطع‌ووصل ۱۵ثانیه‌ای رادیو توسط حلقه همگام‌سازی
**قابلیت:** دکمه «عیب‌یابی موتور صدا» (وضعیت داخلی + ۵۰ خط آخر لاگ)

## v3.1
**رفع باگ:** ناپایداری «اتصال به موتور صدا برقرار نشد» — به‌جای یک اتصال TCP
جدید برای هر دستور (که session های نیمه‌باز انباشته می‌کرد)، یک اتصال دائمی
واحد با صف دستورات و اتصال مجدد خودکار

## v3.0 (بازنویسی کامل)
**رفع دو باگ بحرانی:**
- کرش موتور هنگام استارت: input.external در Liquidsoap 2.x حذف شده بود
  (علت ECONNREFUSED) → input.external.rawaudio
- منبع پیج همیشه «فعال» بود و جلوی رادیو/موزیک را می‌گرفت (mksafe حذف + blank.strip)
**قابلیت‌ها:** پنل ۵ زبانه (فارسی/انگلیسی/روسی/عربی/ترکی) · دکمه‌های کنترل پخش
(قبلی/پخش/توقف/بعدی/میوت) · ستون‌های قابل‌کلیک ایستگاه‌ها و آهنگ‌ها در داشبورد ·
ویرایش ایستگاه + بررسی سلامت و کیفیت لینک استریم (کدک/بیت‌ریت/فرمت) ·
تست پخش صدا از خود موتور · ریبوت سرور و ری‌استارت موتور از پنل ·
زنگ زمان‌بندی‌شده (آپلود فایل + ساعت + روزهای هفته) · همگام‌سازی خودکار وضعیت

## v2.0
**قابلیت‌ها:** پنل چندصفحه‌ای (داشبورد/موزیک/رادیو/تنظیمات) · تنظیمات SIP و
کارت صدا و تایم‌زون از داخل پنل (بدون nano) · ساعت زنده · حالت روز/شب ·
داشبورد گرافیکی: کاور آلبوم (استخراج با ffmpeg)، نام خواننده، نوار پخش، اکولایزر ·
بررسی وضعیت رجیستر SIP

## v1.0 (نسخه اولیه)
- پیجینگ تلفنی SIP: رجیستر روی سانترال موجود (حالت A) یا مستقل (حالت B)،
  پاسخ خودکار + بوق + پل صدای زنده به آمپلی‌فایر
- رادیو آنلاین + پلی‌لیست موزیک محلی با موتور Liquidsoap
- پنل وب با لاگین: انتخاب منبع، کنترل صدا، مدیریت ایستگاه‌ها و آپلود موزیک
- نصب یک‌دستوره (install.sh) + سرویس‌های systemd
